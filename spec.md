create a super minimal saxo client in vanilla script for nodejs. it should be able to: buy, sell, listPortfolio, fetchInstrument, fetchChart (/chart/v3/charts)

- Use native fetch.
- Authorization is done with a Bearer token.
- Isomorphic code that can run in both Node.js and browser environments.
- use ESM modules (import/export).
- Use functional programming style.

client/index.html - a minimal example of how to use the Saxo client in a browser environment. (use vuejs 3 script setup from importmap and picocss)
